import { ProvinceResolver } from './provinceResolver';
import { DistrictResolver } from './districtResolver';
import { WardResolver } from './wardResolver';

export const resolvers = [ProvinceResolver, DistrictResolver, WardResolver];
